<UserControl x:Class="EduMaster.UI.ClassGroups.ClassGroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             FlowDirection="RightToLeft">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,14">
            <Button Content="➕ فوج جديد" Command="{Binding AddCommand}"
                    DockPanel.Dock="Left" Padding="18,8" FontWeight="SemiBold"/>
            <TextBlock Text="الأفواج" FontSize="24" FontWeight="SemiBold"/>
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox ItemsSource="{Binding YearFilters}" DisplayMemberPath="Label"
                      SelectedItem="{Binding SelectedYearFilter}" Padding="10,6"/>
            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Controls:TextBoxHelper.Watermark="🔍 ابحث باسم الفوج أو المادة أو المستوى أو الأستاذ…"
                     Padding="10,8" Margin="12,0,0,0"/>
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Groups}"
                  SelectedItem="{Binding SelectedGroup}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" SelectionMode="Single"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="اسم الفوج" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="المادة" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="المستوى" Binding="{Binding LevelName}" Width="*"/>
                <DataGridTextColumn Header="الشعب" Binding="{Binding StreamsDisplay}" Width="*"/>
                <DataGridTextColumn Header="الأستاذ" Binding="{Binding TeacherFullName, TargetNullValue='—'}" Width="*"/>
                <DataGridTextColumn Header="القاعة" Binding="{Binding RoomName, TargetNullValue='—'}" Width="*"/>
                <DataGridTextColumn Header="المسجَّلون" Binding="{Binding EnrolledDisplay}" Width="Auto"/>
                <DataGridTemplateColumn Header="الحالة" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="فعّال"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Text" Value="معطّل"/>
                                                <Setter Property="Foreground" Value="Gray"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="لا توجد أفواج في هذا الفلتر بعد — اضغط «فوج جديد» لإنشاء أول فوج"
                   Foreground="Gray" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <TextBlock Grid.Row="2" Text="جارٍ التحميل…" Foreground="Gray"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,14,0,0">
            <Button Content="👥 المسجَّلون"  Command="{Binding OpenRosterCommand}" Padding="14,6" Margin="0,0,8,0" FontWeight="SemiBold"/>
            <Button Content="✏️ تعديل"        Command="{Binding EditCommand}"       Padding="14,6" Margin="0,0,8,0"/>
            <Button Content="🚫 تعطيل"       Command="{Binding DeactivateCommand}" Padding="14,6" Margin="0,0,8,0"/>
            <Button Content="✔ تفعيل"        Command="{Binding ActivateCommand}"   Padding="14,6"/>
        </StackPanel>
    </Grid>
</UserControl>