<UserControl x:Class="EduMaster.UI.People.PeopleView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls" FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="الأشخاص" Style="{StaticResource EduPageTitle}"/>
                <TextBlock Text="الدليل الموحد للأشخاص والأدوار وحسابات الدخول" Style="{StaticResource EduBodyMuted}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="↻  تحديث" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButton}" Padding="14,8" Margin="0,0,8,0"/>
                <Button Content="＋  شخص جديد" Command="{Binding AddCommand}" Style="{StaticResource PrimaryButton}" Padding="18,8"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Width="38" Height="38" CornerRadius="19" Background="#EAF3FC" Margin="0,0,10,0">
                    <TextBlock Text="⌕" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource AccentDarkBrush}"/>
                </Border>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Controls:TextBoxHelper.Watermark="ابحث بالاسم أو الهاتف…" Height="40" Padding="10,6"/>
            </Grid>
        </Border>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="330"/>
            </Grid.ColumnDefinitions>
            <Border Style="{StaticResource Card}" Padding="0" Margin="0,0,14,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Padding="16,12" Background="#FAFBFD" BorderBrush="#E8EDF3" BorderThickness="0,0,0,1" CornerRadius="10,10,0,0">
                        <StackPanel>
                            <TextBlock Text="قائمة الأشخاص" Style="{StaticResource EduSubsectionTitle}"/>
                            <TextBlock Text="حدد شخصاً لعرض الحساب والأدوار المتاحة" Style="{StaticResource EduCaption}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                    <Grid Grid.Row="1">
                        <DataGrid ItemsSource="{Binding People}" SelectedItem="{Binding SelectedPerson}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF1F6" VerticalGridLinesBrush="Transparent" BorderThickness="0" RowHeight="50" ColumnHeaderHeight="40">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="الاسم الكامل" Binding="{Binding FullName}" Width="2*"/>
                                <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone, TargetNullValue='—'}" Width="*"/>
                                <DataGridTextColumn Header="الهاتف الثاني" Binding="{Binding Phone2, TargetNullValue='—'}" Width="*"/>
                                <DataGridTemplateColumn Header="الحالة" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="10" Padding="9,3">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource PositiveSoftBrush}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                <Setter Property="Background" Value="#EEF1F5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="فعّال"/>
                                                            <Setter Property="Foreground" Value="{StaticResource PositiveBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                    <Setter Property="Text" Value="معطّل"/>
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style></TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="👥" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="لا يوجد أشخاص" Style="{StaticResource EduBodyStrong}" Margin="0,8,0,0"/>
                            <TextBlock Text="استخدم زر شخص جديد لإضافة أول سجل" Style="{StaticResource EduBodyMuted}" Margin="0,3,0,0"/>
                        </StackPanel>
                        <Border Background="#CCFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ProgressBar IsIndeterminate="True" Width="120" Height="4"/>
                                <TextBlock Text="جارٍ التحميل…" Style="{StaticResource EduBodyMuted}" Margin="0,9,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                    <Border Grid.Row="2" Background="#FAFBFD" BorderBrush="#E8EDF3" BorderThickness="0,1,0,0" Padding="14,11" CornerRadius="0,0,10,10">
                        <WrapPanel>
                            <Button Content="تعديل" Command="{Binding EditCommand}" Style="{StaticResource SecondaryButton}" Padding="14,7" Margin="0,0,8,0"/>
                            <Button Content="تعطيل" Command="{Binding DeactivateCommand}" Style="{StaticResource SecondaryButton}" Padding="14,7" Margin="0,0,8,0"/>
                            <Button Content="تفعيل" Command="{Binding ActivateCommand}" Style="{StaticResource SecondaryButton}" Padding="14,7"/>
                        </WrapPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- تمت إضافة ScrollViewer هنا فقط: عمود ثابت العرض (330) يحوي StackPanel عمودياً بسيطاً،
                 فلا توجد مشكلة انهيار عمود Star كما في حالة EmployeesView سابقاً.
                 هذا يمنع اختفاء الأزرار عند تكبير مقياس العرض (150%) لأن المحتوى الزائد
                 سيصبح قابلاً للتمرير بدل أن يُقصّ خارج حدود الخلية. -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Border Style="{StaticResource Card}" Padding="18" Margin="0,0,0,14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                <Border Width="42" Height="42" CornerRadius="21" Background="#EAF3FC" Margin="0,0,10,0">
                                    <TextBlock Text="🔑" FontSize="19" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="حساب الدخول" Style="{StaticResource EduSubsectionTitle}"/>
                                    <TextBlock Text="الحساب المرتبط بالشخص المحدد" Style="{StaticResource EduCaption}"/>
                                </StackPanel>
                            </StackPanel>
                            <Border Background="#F7F9FC" BorderBrush="#E3E8F0" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="{Binding AccountStatusText}" Style="{StaticResource EduBodyStrong}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding SelectedAccount.Username, StringFormat='اسم المستخدم: {0}', TargetNullValue=''}" Style="{StaticResource EduCaption}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                            <Button Content="إنشاء حساب" Command="{Binding CreateAccountCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,8"/>
                            <Button Content="فك القفل" Command="{Binding UnlockAccountCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,0,8"/>
                            <Button Content="إعادة تعيين كلمة المرور" Command="{Binding ResetPasswordCommand}" Style="{StaticResource SecondaryButton}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource Card}" Padding="18">
                        <StackPanel>
                            <TextBlock Text="إضافة دور" Style="{StaticResource EduSubsectionTitle}"/>
                            <TextBlock Text="أنشئ ملفاً وظيفياً للشخص المحدد دون تكرار بياناته" Style="{StaticResource EduCaption}" TextWrapping="Wrap" Margin="0,3,0,12"/>
                            <Button Content="إضافة كطالب" Command="{Binding AssignStudentRoleCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,0,8"/>
                            <Button Content="إضافة كأستاذ" Command="{Binding AssignTeacherRoleCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,0,8"/>
                            <Button Content="إضافة كموظف" Command="{Binding AssignEmployeeRoleCommand}" Style="{StaticResource SecondaryButton}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>