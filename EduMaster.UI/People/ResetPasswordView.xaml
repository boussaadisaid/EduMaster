<UserControl x:Class="EduMaster.UI.People.ResetPasswordView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" FlowDirection="RightToLeft" Width="500" Height="390">
    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="22,20,22,8">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="2,0,2,18">
                    <Border Width="48" Height="48" CornerRadius="24" Background="{StaticResource WarningSoftBrush}" Margin="0,0,12,0">
                        <TextBlock Text="🔁" FontSize="21" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="إعادة تعيين كلمة المرور" Style="{StaticResource EduSectionTitle}"/>
                        <TextBlock Text="إنشاء كلمة مرور مؤقتة جديدة" Style="{StaticResource EduBodyMuted}" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
                <Border Style="{StaticResource Card}" Padding="18">
                    <StackPanel>
                        <TextBlock Text="الحساب المحدد" Style="{StaticResource EduFieldLabel}"/>
                        <Border Background="#F7F9FC" CornerRadius="8" Padding="12,9" Margin="0,0,0,16">
                            <TextBlock Text="{Binding PersonName}" Style="{StaticResource EduBodyStrong}"/>
                        </Border>
                        <TextBlock Text="كلمة المرور المؤقتة الجديدة *" Style="{StaticResource EduFieldLabel}"/>
                        <PasswordBox x:Name="TxtNewPassword" PasswordChanged="NewPassword_Changed" Height="40" Padding="10,6"/>
                        <TextBlock Text="سيُطلب من المستخدم تغييرها عند تسجيل الدخول المقبل." Style="{StaticResource EduCaption}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="1" Background="White" BorderBrush="#E3E8F0" BorderThickness="0,1,0,0" Padding="22,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Background="{StaticResource NegativeSoftBrush}" BorderBrush="#F1B8BE" BorderThickness="1" CornerRadius="8" Padding="12,9" Margin="0,0,14,0" Visibility="{Binding HasErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource NegativeBrush}" TextWrapping="Wrap"/>
                </Border>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="إعادة التعيين" Command="{Binding ResetCommand}" Style="{StaticResource PrimaryButton}" Padding="18,9" Margin="0,0,8,0"/>
                    <Button Content="إلغاء" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}" Padding="18,9"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>