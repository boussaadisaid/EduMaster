<UserControl x:Class="EduMaster.UI.Sms.SmsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EduMaster.UI.Sms"
             FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,18">
            <TextBlock Text="الرسائل SMS" Style="{StaticResource EduPageTitle}"/>
            <TextBlock Text="إرسال الرسائل من هاتف Android المربوط بـTextBee ومتابعة حالة التسليم" Style="{StaticResource EduBodyMuted}" Margin="0,4,0,0"/>
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="إرسال رسالة">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Style="{StaticResource Card}" Padding="14" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="مصدر المستلمين" Style="{StaticResource EduFieldLabel}" VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding RecipientSources}" SelectedItem="{Binding RecipientSource}" DisplayMemberPath="DisplayName" Height="34"/>
                            <Button Grid.Column="2" Content="تحميل المستلمين" Command="{Binding LoadRecipientsCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Padding="14,7"/>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="250"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="250"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="بحث" Style="{StaticResource EduFieldLabel}" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Height="34" Padding="8" ToolTip="ابحث في المصدر المحدد ثم اضغط تحميل المستلمين"/>
                            <TextBlock Grid.Column="2" Text="القالب" Style="{StaticResource EduFieldLabel}" VerticalAlignment="Center" Margin="16,0,0,0"/>
                            <ComboBox Grid.Column="3" ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}" DisplayMemberPath="Name" Height="34"/>
                            <Button Grid.Column="4" Content="📝 إدارة القوالب" Command="{Binding ManageTemplatesCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Padding="12,7" HorizontalAlignment="Left"/>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="150"/>
                        </Grid.RowDefinitions>
                        <Border Style="{StaticResource Card}" Padding="0" Margin="0,0,0,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Background="#FAFBFD" BorderBrush="#E8EDF3" BorderThickness="0,0,0,1" Padding="12,9">
                                    <DockPanel>
                                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                            <TextBlock Text="المحدد: " Style="{StaticResource EduBodyMuted}"/>
                                            <TextBlock Text="{Binding SelectedRecipientCount}" Style="{StaticResource EduBodyStrong}"/>
                                            <TextBlock Text=" / " Style="{StaticResource EduBodyMuted}"/>
                                            <TextBlock Text="{Binding RecipientCount}" Style="{StaticResource EduBodyStrong}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Content="تحديد الكل" Command="{Binding SelectAllRecipientsCommand}" Style="{StaticResource SecondaryButton}" Padding="12,6" Margin="0,0,6,0"/>
                                            <Button Content="إلغاء التحديد" Command="{Binding ClearRecipientsCommand}" Style="{StaticResource SecondaryButton}" Padding="12,6"/>
                                        </StackPanel>
                                    </DockPanel>
                                </Border>
                                <DataGrid Grid.Row="1" ItemsSource="{Binding Recipients}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" RowHeight="40">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="" Width="46">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="الاسم" Binding="{Binding FullName}" Width="2*"/>
                                        <DataGridTextColumn Header="الولي" Binding="{Binding GuardianName, TargetNullValue='—'}" Width="*"/>
                                        <DataGridTextColumn Header="الهاتف" Binding="{Binding PhoneNumber}" Width="150"/>
                                        <DataGridTextColumn Header="المبلغ" Binding="{Binding AmountText, TargetNullValue='—'}" Width="110"/>
                                        <DataGridTextColumn Header="الحصص" Binding="{Binding RemainingSessionsText, TargetNullValue='—'}" Width="80"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="نص الرسالة" Style="{StaticResource EduFieldLabel}" VerticalAlignment="Top" Margin="0,5,8,0"/>
                                <TextBox Grid.Column="1" Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MaxLength="1000" Padding="10"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <Border Grid.Row="3" Background="#FAFBFD" BorderBrush="#E8EDF3" BorderThickness="0,1,0,0" Padding="12,10" Margin="0,0,0,0">
                        <DockPanel>
                            <TextBlock Text="يتم استبدال المتغيرات تلقائيًا قبل الإرسال، مثل {StudentName} و{Amount} و{SchoolName}." Style="{StaticResource EduCaption}" VerticalAlignment="Center"/>
                            <Button Content="📤 إرسال الرسائل المحددة" Command="{Binding SendSelectedCommand}" Style="{StaticResource PrimaryButton}" Padding="18,9" DockPanel.Dock="Left"/>
                        </DockPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="القوالب">
                <Grid Margin="12">
                    <Border Style="{StaticResource Card}" Padding="0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <DataGrid Grid.Row="0" ItemsSource="{Binding Templates}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectedItem="{Binding SelectedTemplate}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="اسم القالب" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="النوع" Binding="{Binding CategoryText}" Width="150"/>
                                    <DataGridTextColumn Header="النص" Binding="{Binding Body}" Width="2*"/>
                                    <DataGridTemplateColumn Header="الحالة" Width="90">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding IsActive, Converter={StaticResource BooleanToArabicTextConverter}}" HorizontalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="12,10">
                                <Button Content="📝 إدارة القوالب" Command="{Binding ManageTemplatesCommand}" Style="{StaticResource PrimaryButton}" Padding="16,8"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="إعداد الخدمة">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="مفتاح TextBee API" Style="{StaticResource EduFieldLabel}" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" x:Name="ApiKeyBox" Height="32" Padding="8" PasswordChanged="ApiKeyBox_OnPasswordChanged"/>
                        <TextBlock Text="جهاز الإرسال" Style="{StaticResource EduFieldLabel}" Grid.Row="1" VerticalAlignment="Center" Margin="0,12,0,0"/>
                        <Grid Grid.Column="1" Grid.Row="1" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}" DisplayMemberPath="DisplayName" Height="32" ToolTip="الهاتف الذي يحتوي على شريحة الإرسال والمربوط بحساب TextBee."/>
                            <Button Grid.Column="1" Content="تحديث الأجهزة" Command="{Binding RefreshDevicesCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                        </Grid>
                        <TextBlock Text="الحالة" Style="{StaticResource EduFieldLabel}" Grid.Row="2" VerticalAlignment="Center" Margin="0,12,0,0"/>
                        <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding ConnectionText}" Style="{StaticResource EduBodyStrong}" Margin="0,12,0,0"/>
                        <StackPanel Grid.Column="1" Grid.Row="3" Orientation="Horizontal" Margin="0,18,0,0">
                            <Button Content="حفظ الإعدادات" Command="{Binding SaveSettingsCommand}" Style="{StaticResource PrimaryButton}"/>
                            <TextBlock Text="رقم الهاتف المرسل لا يُدخل هنا؛ TextBee يرسل من شريحة الهاتف المحددة." Style="{StaticResource EduCaption}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>
                        <Border Grid.Row="4" Grid.ColumnSpan="2" Style="{StaticResource Card}" Margin="0,20,0,0">
                            <StackPanel>
                                <TextBlock Text="طريقة العمل" Style="{StaticResource EduSubsectionTitle}"/>
                                <TextBlock Text="الهاتف هو بوابة SMS الفعلية. يجب أن يبقى التطبيق مفتوحًا والهاتف متصلاً بالإنترنت ومزوّدًا بشريحة قادرة على إرسال SMS." Style="{StaticResource EduBodyMuted}" TextWrapping="Wrap" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="اختبار الإرسال">
                <Grid Margin="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="رسالة اختبار" Style="{StaticResource EduSubsectionTitle}"/>
                    <Grid Grid.Row="1" Margin="0,14,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="رقم المستلم" Style="{StaticResource EduFieldLabel}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding TestPhone, UpdateSourceTrigger=PropertyChanged}" Height="32" Padding="8"/>
                    </Grid>
                    <StackPanel Grid.Row="2">
                        <TextBlock Text="نص الرسالة" Style="{StaticResource EduFieldLabel}"/>
                        <TextBox Text="{Binding TestMessage, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="180" Padding="10"/>
                    </StackPanel>
                    <Button Grid.Row="3" Content="📩 إرسال رسالة اختبار" Command="{Binding SendTestCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Padding="20,9"/>
                </Grid>
            </TabItem>

            <TabItem Header="السجل">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="تحديث" Command="{Binding RefreshHistoryCommand}" Style="{StaticResource SecondaryButton}"/>
                        <Button Content="مزامنة الحالة" Command="{Binding SyncSelectedCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding History}" SelectedItem="{Binding SelectedHistory}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding CreatedAtUtc, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="Auto"/>
                            <DataGridTextColumn Header="الهاتف" Binding="{Binding PhoneNumber}" Width="150"/>
                            <DataGridTextColumn Header="الرسالة" Binding="{Binding MessageBody}" Width="2*"/>
                            <DataGridTextColumn Header="الحالة" Binding="{Binding StatusText}" Width="110"/>
                            <DataGridTextColumn Header="المزوّد" Binding="{Binding ProviderStatus, TargetNullValue=—}" Width="110"/>
                            <DataGridTextColumn Header="الخطأ" Binding="{Binding LastErrorMessage, TargetNullValue=—}" Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
