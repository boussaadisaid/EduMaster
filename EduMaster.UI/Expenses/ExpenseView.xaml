<UserControl x:Class="EduMaster.UI.Expenses.ExpenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="المصاريف التشغيلية" Style="{StaticResource EduPageTitle}"/>
                <TextBlock Text="تسجيل ومتابعة مصاريف المدرسة حسب السنة الدراسية والفئة" Style="{StaticResource EduBodyMuted}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="الفئات" Command="{Binding ManageCategoriesCommand}" Style="{StaticResource SecondaryButton}" Padding="14,8" Margin="0,0,8,0"/>
                <Button Content="＋  مصروف جديد" Command="{Binding NewExpenseCommand}" Style="{StaticResource PrimaryButton}" Padding="16,8"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Padding="16,12" Background="#FAFBFD" BorderBrush="#E8EDF3" BorderThickness="0,0,0,1" CornerRadius="10,10,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="210"/>
                            <ColumnDefinition Width="210"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="230"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="السنة الدراسية" Style="{StaticResource EduFieldLabel}"/>
                            <ComboBox ItemsSource="{Binding AcademicYears}" SelectedItem="{Binding SelectedAcademicYear}" DisplayMemberPath="Name" Height="38"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="الفئة" Style="{StaticResource EduFieldLabel}"/>
                            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name" Height="38"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="10,0,0,0">
                            <TextBlock Text="من" Style="{StaticResource EduFieldLabel}"/>
                            <DatePicker SelectedDate="{Binding FromDate}" SelectedDateFormat="Short" Language="ar-DZ" Height="38"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Margin="10,0,0,0">
                            <TextBlock Text="إلى" Style="{StaticResource EduFieldLabel}"/>
                            <DatePicker SelectedDate="{Binding ToDate}" SelectedDateFormat="Short" Language="ar-DZ" Height="38"/>
                        </StackPanel>

                        <Button Grid.Column="4" Content="↻" ToolTip="تحديث" Command="{Binding RefreshCommand}"
                                Style="{StaticResource SecondaryButton}" Width="38" Height="38" Padding="0"
                                Margin="8,17,0,0" VerticalAlignment="Top"/>

                        <Border Grid.Column="5" Margin="18,0,0,0" Padding="14,7"
                                Background="{StaticResource AccentBrush}" CornerRadius="8"
                                HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock Text="{Binding TotalText}" Foreground="White" Style="{StaticResource EduBodyStrong}"/>
                        </Border>
                    </Grid>
                </Border>
                <Grid Grid.Row="1">
                    <DataGrid ItemsSource="{Binding Expenses}" SelectedItem="{Binding SelectedExpense}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF1F6" VerticalGridLinesBrush="Transparent" BorderThickness="0" RowHeight="46" ColumnHeaderHeight="38">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding ExpenseDate, StringFormat='yyyy-MM-dd'}" Width="Auto"/>
                            <DataGridTextColumn Header="الفئة" Binding="{Binding CategoryName}" Width="1.2*"/>
                            <DataGridTextColumn Header="المبلغ" Binding="{Binding AmountCentimes, Converter={StaticResource CentimesToDinarsConverter}}" Width="Auto"/>
                            <DataGridTextColumn Header="الملاحظة" Binding="{Binding Note, TargetNullValue='—'}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock Text="لا توجد مصاريف ضمن الفلاتر الحالية" Style="{StaticResource EduBodyMuted}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <Border Background="#CCFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="جارٍ تحميل المصاريف…" Style="{StaticResource EduBodyMuted}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
