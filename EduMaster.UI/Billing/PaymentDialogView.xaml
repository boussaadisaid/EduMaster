<UserControl x:Class="EduMaster.UI.Billing.PaymentDialogView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" FlowDirection="RightToLeft" Width="740" MaxHeight="650">
    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="22,20,22,16">
            <Border Width="48" Height="48" CornerRadius="24" Background="{StaticResource PositiveSoftBrush}" Margin="0,0,12,0">
                <TextBlock Text="💵" FontSize="21" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="قبض دفعة" Style="{StaticResource EduSectionTitle}"/>
                <TextBlock Text="{Binding ContextText}" Style="{StaticResource EduBodyMuted}" Margin="0,2,0,0"/>
            </StackPanel>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="22,0,22,10">
            <StackPanel>
                <Border Style="{StaticResource Card}" Padding="18" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="بيانات القبض" Style="{StaticResource EduSubsectionTitle}" Margin="0,0,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,7,14">
                                <TextBlock Text="المبلغ (دج) *" Style="{StaticResource EduFieldLabel}"/>
                                <TextBox Text="{Binding AmountText, UpdateSourceTrigger=PropertyChanged}" Height="40" Padding="10,6"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="7,0,0,14">
                                <TextBlock Text="تاريخ القبض *" Style="{StaticResource EduFieldLabel}"/>
                                <DatePicker SelectedDate="{Binding PaidOn}" Height="40"/>
                            </StackPanel>
                        </Grid>
                        <CheckBox Content="{Binding GuardianOptionText}" IsChecked="{Binding PaidByGuardian}" Margin="0,0,0,14" Visibility="{Binding ShowGuardianOption, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <TextBlock Text="ملاحظة اختيارية" Style="{StaticResource EduFieldLabel}"/>
                        <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" Height="40" Padding="10,6"/>
                        <Border Background="{StaticResource PositiveSoftBrush}" CornerRadius="8" Padding="12,9" Margin="0,12,0,0" Visibility="{Binding HasCredit, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="{Binding CreditText}" Foreground="{StaticResource PositiveBrush}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Padding="0" Visibility="{Binding HasOpenCharges, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="220"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Padding="16,12" Background="#FAFBFD" BorderBrush="#E8EDF3" BorderThickness="0,0,0,1" CornerRadius="10,10,0,0">
                            <StackPanel>
                                <TextBlock Text="تخصيص الدفعة" Style="{StaticResource EduSubsectionTitle}"/>
                                <TextBlock Text="اقتراح تلقائي بالأقدم أولاً، ويمكن تعديل كل مبلغ" Style="{StaticResource EduCaption}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF1F6" VerticalGridLinesBrush="Transparent" BorderThickness="0" RowHeight="45" ColumnHeaderHeight="38">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="المستحق" Binding="{Binding SourceText}" Width="2*" IsReadOnly="True"/>
                                <DataGridTextColumn Header="النوع" Binding="{Binding KindText}" Width="Auto" IsReadOnly="True"/>
                                <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingText}" Width="Auto" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="التخصيص (دج)" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding AllocatedText, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0,0,0,1" Padding="6,3"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Border Grid.Row="2" Background="{StaticResource WarningSoftBrush}" Padding="12,9" CornerRadius="0,0,10,10" Visibility="{Binding HasUnallocatedFromThis, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="{Binding UnallocatedFromThisText}" Foreground="{StaticResource WarningBrush}" TextWrapping="Wrap"/>
                        </Border>
                    </Grid>
                </Border>
                <Border Background="#EEF5FC" BorderBrush="#D5E5F4" BorderThickness="1" CornerRadius="8" Padding="12,9" Margin="0,14,0,0" Visibility="{Binding NoOpenCharges, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="لا توجد مستحقات مفتوحة؛ ستسجل الدفعة كلها كزائدة دائنة للطالب." Style="{StaticResource EduCaption}" Foreground="{StaticResource AccentDarkBrush}" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="2" Background="White" BorderBrush="#E3E8F0" BorderThickness="0,1,0,0" Padding="22,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Background="{StaticResource NegativeSoftBrush}" BorderBrush="#F1B8BE" BorderThickness="1" CornerRadius="8" Padding="12,9" Margin="0,0,14,0" Visibility="{Binding HasErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource NegativeBrush}" TextWrapping="Wrap"/>
                </Border>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="تثبيت القبض" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Padding="20,9" Margin="0,0,8,0"/>
                    <Button Content="إلغاء" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}" Padding="20,9"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>