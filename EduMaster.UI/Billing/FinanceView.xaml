<UserControl x:Class="EduMaster.UI.Billing.FinanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tabs="clr-namespace:EduMaster.UI.Billing.Tabs"
             FlowDirection="RightToLeft">

    <UserControl.Resources>

        <!-- كل ViewModel داخل Tabs يُعرض بالـView المقابل له -->
        <DataTemplate DataType="{x:Type tabs:DebtsTabViewModel}">
            <tabs:DebtsTabView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type tabs:PaymentsTabViewModel}">
            <tabs:PaymentsTabView/>
        </DataTemplate>

    </UserControl.Resources>

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- رأس الصفحة -->
        <Grid Margin="0,0,0,18">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>

                <TextBlock Text="المالية"
                           Style="{StaticResource EduPageTitle}"/>

                <TextBlock Text="متابعة الديون وسجل القبض والصرف والإيصالات"
                           Style="{StaticResource EduBodyMuted}"
                           Margin="0,4,0,0"/>

            </StackPanel>

            <Button Grid.Column="1"
                    Content="↻  تحديث"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource SecondaryButton}"
                    Padding="14,8"/>

        </Grid>

        <!-- التبويبات -->
        <TabControl Grid.Row="1"
                    ItemsSource="{Binding Tabs}"
                    SelectedItem="{Binding SelectedTab}"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch">

            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayName}"
                               FontFamily="{StaticResource EduFontSemiBold}"
                               FontWeight="Normal"/>
                </DataTemplate>
            </TabControl.ItemTemplate>

        </TabControl>

    </Grid>

</UserControl>